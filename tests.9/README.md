# V9 Tests

Test project, mini, rft, vmc, ecc scripts which are new features in Pyarmor 9+

Make sure package `pyarmor.cli>=9.2.2` and `pyarmor.mini>=3.0` has been installed:

    python accept_test.py
